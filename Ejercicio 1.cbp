#include <stdio.h>

int main() {
    int x = 10;
    int *p = &x;  // p almacena la dirección de x

    printf("Valor de x: %d\n", x);
    printf("Dirección de x: %p\n", (void*)p);

    return 0;
}
